'use client';import {useEffect,useState} from 'react';
export default function AnalyticsPage(){const[d,setD]=useState<any>();useEffect(()=>{fetch('/api/analytics?days=30').then(r=>r.json()).then(setD)},[]);return <div className="mx-auto max-w-6xl"><div className="rounded-3xl border bg-white p-6 shadow-sm"><h1 className="text-2xl font-bold">Analytics</h1><p className="text-sm text-slate-500">Last 30 days</p><div className="mt-6 grid gap-3 md:grid-cols-3">{['sales','expenses','grossProfit'].map(k=><div key={k} className="rounded-2xl border p-4"><p className="text-sm text-slate-500">{k}</p><p className="mt-1 text-2xl font-bold">৳{Number((d?.series||[]).reduce((n:any,x:any)=>n+(x[k]||0),0)).toLocaleString('en-BD')}</p></div>)}</div><div className="mt-6 space-y-2">{d?.series?.map((x:any)=><div key={x.date} className="grid grid-cols-4 rounded-xl border p-3 text-sm"><span>{x.date}</span><span>Sale ৳{x.sales.toLocaleString()}</span><span>Expense ৳{x.expenses.toLocaleString()}</span><span>Profit ৳{x.netProfit.toLocaleString()}</span></div>)}</div></div></div>}
