'use client';import {useState} from 'react';import {Bot,Send,Volume2} from 'lucide-react';
export default function AssistantPage(){const[q,setQ]=useState('');const[a,setA]=useState('');const[loading,setLoading]=useState(false);const ask=async(e:React.FormEvent)=>{e.preventDefault();if(!q.trim())return;setLoading(true);const r=await fetch('/api/assistant',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q})});const d=await r.json();setA(d.answer||d.error);setLoading(false)};return <div className="mx-auto max-w-3xl"><div className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><div className="rounded-2xl bg-emerald-100 p-3 text-emerald-700"><Bot/></div><div><h1 className="text-2xl font-bold">Smart Shop Assistant</h1><p className="text-sm text-slate-500">Ask about today’s business in Bengali or English.</p></div></div><form onSubmit={ask} className="mt-6 flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="আজকের বিক্রি কেমন?" className="input flex-1"/><button className="btn-primary" disabled={loading}><Send size={16}/></button></form>{a&&<div className="mt-5 rounded-2xl bg-slate-50 p-5"><p className="leading-7">{a}</p><button className="mt-3 text-sm text-emerald-700" onClick={()=>{const u=new SpeechSynthesisUtterance(a);u.lang='bn-BD';speechSynthesis.speak(u)}}><Volume2 size={16} className="mr-1 inline"/> শুনুন</button></div>}</div></div>}
