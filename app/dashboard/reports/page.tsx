'use client';
import {useEffect,useState} from 'react';import {BarChart3,TrendingUp,Wallet,Package} from 'lucide-react';
export default function ReportsPage(){const[r,setR]=useState<any>(null);const load=()=>fetch('/api/reports').then(x=>x.json()).then(setR);useEffect(()=>{load()},[]);if(!r)return <p>Loading reports…</p>;const s=r.summary;return <div className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-emerald-700">Business intelligence</p><h1 className="text-3xl font-bold">রিপোর্ট</h1><p className="mt-2 text-slate-500">Sales, cost, profit, cash collections and outstanding balances.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat icon={<TrendingUp/>} title="Sales" value={s.sales}/><Stat icon={<Package/>} title="Cost of goods" value={s.costOfGoods}/><Stat icon={<BarChart3/>} title="Gross profit" value={s.grossProfit}/><Stat icon={<Wallet/>} title="Net profit" value={s.netProfit}/></div><div className="mt-6 grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Cash movement</h2><div className="mt-4 space-y-3"><Row label="Cash sales" value={s.cashSales}/><Row label="Customer collections" value={s.customerCollections}/><Row label="Expenses" value={-s.expense}/><Row label="Supplier payments" value={-s.supplierPayments}/></div></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Top products</h2><div className="mt-4 space-y-3">{r.topProducts.map((p:any)=><div key={p._id} className="flex justify-between rounded-xl border p-3"><span>{p.product?.name||'Deleted product'} <small className="text-slate-400">({p.quantity})</small></span><strong>৳{p.sales.toLocaleString()}</strong></div>)}</div></section></div><section className="mt-5 rounded-2xl border bg-white p-5"><h2 className="font-semibold">Outstanding parties</h2><div className="mt-4 grid gap-2 md:grid-cols-2">{r.partyTotals.map((p:any)=><div key={p._id} className="flex justify-between rounded-xl border p-3"><span>{p.name} <small className="text-slate-400">{p.partyType}</small></span><strong className={p.currentBalance>=0?'text-emerald-700':'text-red-600'}>৳{Math.abs(p.currentBalance||0).toLocaleString()}</strong></div>)}</div></section></div>}
function Stat({icon,title,value}:{icon:React.ReactNode;title:string;value:number}){return <div className="rounded-2xl border bg-white p-5"><div className="text-emerald-600">{icon}</div><p className="mt-3 text-sm text-slate-500">{title}</p><p className="mt-1 text-2xl font-bold">৳{Number(value||0).toLocaleString()}</p></div>}function Row({label,value}:{label:string;value:number}){return <div className="flex justify-between border-b pb-2"><span className="text-slate-500">{label}</span><strong>৳{Number(value||0).toLocaleString()}</strong></div>}
