import { z } from 'zod';
export const PartyInputSchema=z.object({name:z.string().trim().min(2).max(150),phone:z.string().trim().max(30).nullable().optional(),partyType:z.enum(['CUSTOMER','SUPPLIER'])});
export const ProductInputSchema=z.object({name:z.string().trim().min(1).max(150),unit:z.string().trim().min(1).max(30),stockQuantity:z.coerce.number().min(0),buyPrice:z.coerce.number().min(0),sellPrice:z.coerce.number().min(0),lowStockThreshold:z.coerce.number().min(0)});
export const ProductPatchSchema=ProductInputSchema.partial(); export const PartyPatchSchema=PartyInputSchema.partial();
export const TransactionInputSchema=z.object({type:z.enum(['DUE_GIVEN','DUE_RECEIVED','STOCK_IN','STOCK_OUT','EXPENSE']),partyId:z.string().optional().nullable(),productId:z.string().optional().nullable(),amount:z.coerce.number().finite().min(0),quantity:z.coerce.number().finite().min(0),unitPrice:z.coerce.number().finite().min(0).optional(),paidAmount:z.coerce.number().finite().min(0).optional(),paymentMethod:z.enum(['CASH','BANK','OTHER']).optional(),notes:z.string().trim().max(500).optional(),timestamp:z.coerce.date().optional()}).superRefine((v,ctx)=>{if(['DUE_GIVEN','DUE_RECEIVED'].includes(v.type)&&(!v.partyId||v.amount<=0))ctx.addIssue({code:'custom',path:['partyId'],message:'Party and positive amount are required'});if(['STOCK_IN','STOCK_OUT'].includes(v.type)&&(!v.productId||v.quantity<=0))ctx.addIssue({code:'custom',path:['productId'],message:'Product and positive quantity are required'});if(v.type==='EXPENSE'&&v.amount<=0)ctx.addIssue({code:'custom',path:['amount'],message:'Expense amount must be positive'});});
export type TransactionInput=z.infer<typeof TransactionInputSchema>;
export const SaleLineSchema=z.object({productId:z.string().min(1),quantity:z.coerce.number().positive(),unitPrice:z.coerce.number().nonnegative().optional()});
export const SaleInputSchema=z.object({partyId:z.string().nullable().optional(),lines:z.array(SaleLineSchema).min(1).max(100),paidAmount:z.coerce.number().min(0),paymentMethod:z.enum(['CASH','BANK','OTHER']).default('CASH'),notes:z.string().trim().max(500).optional(),timestamp:z.coerce.date().optional()});
export type SaleInput=z.infer<typeof SaleInputSchema>;
export const PurchaseInputSchema=z.object({partyId:z.string().nullable().optional(),lines:z.array(SaleLineSchema).min(1).max(100),paidAmount:z.coerce.number().min(0),paymentMethod:z.enum(['CASH','BANK','OTHER']).default('CASH'),notes:z.string().trim().max(500).optional(),timestamp:z.coerce.date().optional()});
export type PurchaseInput=z.infer<typeof PurchaseInputSchema>;
export const PaymentInputSchema=z.object({partyId:z.string().min(1),amount:z.coerce.number().positive(),paymentMethod:z.enum(['CASH','BANK','OTHER']).default('CASH'),notes:z.string().trim().max(500).optional(),timestamp:z.coerce.date().optional()});
export type PaymentInput=z.infer<typeof PaymentInputSchema>;
export const ClosingInputSchema=z.object({dateKey:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),openingCash:z.coerce.number().min(0),adjustments:z.coerce.number().default(0),notes:z.string().trim().max(500).optional()});
